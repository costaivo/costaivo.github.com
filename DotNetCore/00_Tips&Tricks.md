# Tips and Tricks - DotNet Core

## DotNet Core Commands

1. Create console app
    `dotnet new`

1. Create web app
    `dotnet new -t web`

1. Restore packages
    `dotnet restore`

1. Run application
    `dotnet run`
